DESCRIPTION="C/C++/ObjC JIT interpreter"
HOMEPAGE="http://ccod.sourceforge.net/"
SRC_URI="mirror://sourceforge/ccod/${P}.tar.gz"
SRC_DIR=.

src_compile() {
	lndirs
	cd ${B}
	rm -f Makefile
	./configure
	cygmake
}

src_install() {
	cd ${B}
	dodir /usr/{bin,include,lib}
	cyginstall PREFIX=${D}/usr
}
